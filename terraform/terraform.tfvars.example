# Copy this to terraform.tfvars and fill in your project_id.
# terraform.tfvars is gitignored — never commit it to a public repo.

project_id = "your-gcp-project-id"
region     = "us-central1"
zone       = "us-central1-a"
hf_token   = "hf_your_actual_token_here"